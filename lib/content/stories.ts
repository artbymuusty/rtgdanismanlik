import type { StudentStory } from "./types";

/**
 * Real student journeys. Empty until a real student's story exists — with
 * their consent — see the `StudentStory` type for the required shape.
 * Do NOT fill this with an invented testimonial; every page that reads
 * this array is written to render nothing when it's empty.
 */
export const studentStories: StudentStory[] = [];
