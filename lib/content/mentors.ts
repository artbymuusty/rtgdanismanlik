import type { Mentor } from "./types";

/**
 * Real mentor profiles. Empty until real people, with their own consent and
 * real information, are added — see the `Mentor` type for the required
 * shape. Do NOT fill this with invented people; every page that reads this
 * array is written to render nothing when it's empty rather than fall back
 * to placeholder content.
 */
export const mentors: Mentor[] = [];
